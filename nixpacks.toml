providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11.8"

# BURAYI EKLİYORUZ: İşletim sistemine gerekli olan C++ kütüphanesini kuruyoruz
[phases.setup]
nixPkgs = ["...", "stdenv.cc.cc.lib"]

[phases.build]
cmds = ["echo 'Build phase - skipping Django commands'"]

[start]
cmd = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py setup_auth --admin-username=admin --admin-password=admin --admin-email=gokdemirbahadir0@gmail.com && gunicorn core.wsgi --bind 0.0.0.0:$PORT"